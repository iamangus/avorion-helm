apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "avorion.fullname" . }}-configmap
data:
  temp-server.ini: |
    [Game]
  {{- range $key, $value := .Values.settingsINI }}
    {{ $key }}={{ $value }}
  {{- end }}
